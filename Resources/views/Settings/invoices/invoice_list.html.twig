<div class="card">
    <div class="card-header">Invoices</div>
    <div class="card-block">
        {% if invoices is not empty %}
            {# TODO: Display customer's invoices #}
        {% else %}
            <div class="alert alert-info m-b-0">
                There are no invoices for your account.
            </div>
        {% endif %}
    </div>
</div>
